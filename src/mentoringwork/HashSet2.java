package mentoringwork;

public class HashSet2 {

    // Hash set only contain unique data
    // Won't print or perform duplicate data
    // Don't have get in order to get "get" we need to use Iterator...need to understand more
}
